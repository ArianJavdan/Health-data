# Implemented in: features/planned/06-daily-mart.md
# Joins all metrics into one row per day, computes recovery_score (0-100),
# recovery_status label, and training_recommendation label. Writes to DuckDB.
