from dagster import Definitions

# Assets and resources are registered here as each feature is implemented.
# See features/planned/ for the implementation roadmap.

defs = Definitions(
    assets=[],
    resources={},
)
